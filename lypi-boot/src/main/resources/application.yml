spring:
  config:
    import: optional:file:${user.home}/.ly-pi/application.yml
  main:
    banner-mode: off

lypi:
  ai:
    default-provider:
    default-model:
    compaction-summary:
      fallback-policy: fallback_deterministic
    providers: {}
