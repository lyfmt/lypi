spring:
  main:
    banner-mode: off

lypi:
  ai:
    default-provider:
    default-model:
    compaction-summary:
      enabled: false
      fallback-policy: fallback_deterministic
    providers: {}
