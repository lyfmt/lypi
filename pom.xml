<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.0.0</version>
		<relativePath/>
	</parent>

	<groupId>cn.lycode</groupId>
	<artifactId>ly-code</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>ly-code</name>
	<description>ly-code coding agent framework</description>

	<modules>
		<module>lycode-contracts</module>
		<module>lycode-session</module>
		<module>lycode-agent-core</module>
		<module>lycode-ai</module>
		<module>lycode-tool</module>
		<module>lycode-security</module>
		<module>lycode-resource</module>
		<module>lycode-runtime</module>
		<module>lycode-transport-headless</module>
		<module>lycode-transport-tui</module>
		<module>lycode-boot</module>
	</modules>

	<properties>
		<java.version>21</java.version>
		<maven.version>3.9.0</maven.version>
		<maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-contracts</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-session</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-agent-core</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-ai</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-tool</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-security</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-resource</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-runtime</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-transport-headless</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>cn.lycode</groupId>
				<artifactId>lycode-transport-tui</artifactId>
				<version>${project.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<version>${maven-enforcer-plugin.version}</version>
				<executions>
					<execution>
						<id>enforce-build-environment</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireJavaVersion>
									<version>[${java.version},22)</version>
								</requireJavaVersion>
								<requireMavenVersion>
									<version>[${maven.version},)</version>
								</requireMavenVersion>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
